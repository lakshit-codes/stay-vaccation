import { MasterActivitiesPage } from "@/app/components/AdminCore";

export const metadata = {
  title: "Master Activities — Stay Vacation",
  description: "Manage reusable activities linked across multiple packages.",
};


export default function Page() {
  return <MasterActivitiesPage />;
}
