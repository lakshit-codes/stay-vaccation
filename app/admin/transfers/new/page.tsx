import NewTransferContent from "./NewTransferContent";

export const metadata = {
  title: "New Transfer Route — Stay Vacation",
  description: "Add a new reusable transfer route to the system.",
};

export default function NewTransferPage() {
  return <NewTransferContent />;
}
