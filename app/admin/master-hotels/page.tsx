import { MasterHotelsPage } from "@/app/components/AdminCore";

export const metadata = {
  title: "Master Hotels — Stay Vacation",
  description: "Manage the global hotel catalog with two-way sync.",
};


export default function Page() {
  return <MasterHotelsPage />;
}
